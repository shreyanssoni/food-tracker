# Groq (optional; can be primary when AI_PROVIDER=groq)
GROQ_API_KEY=your-groq-api-key
# Optional model override, defaults to llama-3.1-8b-instant
GROQ_MODEL=llama-3.1-8b-instant

# Copy to .env.local and fill in your values

# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Google OAuth (for NextAuth)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-use-openssl-rand-base64-32

# Google Gemini API
GEMINI_API_KEY=your-gemini-api-key

# Unsplash API (optional; enables curated gym images on /me)
UNSPLASH_ACCESS_KEY=your-unsplash-access-key

# AI Provider selection (priority when AI_PROVIDER=auto is: OpenRouter -> Gemini -> Groq)
# Set to one of: auto | gemini | openrouter | groq
AI_PROVIDER=auto

# Debug mode: when true, bypass external AI calls and return canned defaults
# Useful for local development without API keys
AI_DEBUG=false

# OpenRouter (primary in auto mode if key is present)
OPENROUTER_API_KEY=your-openrouter-api-key
# Single model (optional). Example: meta-llama/llama-3.1-8b-instruct:free
OPENROUTER_MODEL=
# Rotating models (comma-separated). If provided, overrides OPENROUTER_MODEL.
# Replace the placeholders below with the exact OpenRouter slugs for the free models you want:
# - Google: Gemini 2.5 Flash Image Preview (free)
# - NVIDIA: Llama 3.1 Nemotron Ultra 253B v1 (free)
# - DeepSeek: R1 0528 (free)
# - OpenAI: gpt-oss-20b (free)
# - Mistral: Mistral Small 3.2 24B (free)
# Example (with real slugs): google/gemini-2.5-flash-preview:free, nvidia/llama-3.1-nemotron-ultra-253b:free, deepseek/r1-0528:free, openai/gpt-oss-20b:free, mistralai/mistral-small-3.2-24b:free
OPENROUTER_MODELS=google/gemini-2.5-flash-preview:free, nvidia/llama-3.1-nemotron-ultra-253b:free, deepseek/r1-0528:free, openai/gpt-oss-20b:free, mistralai/mistral-small-3.2-24b:free
OPENROUTER_REFERRER=http://localhost
OPENROUTER_APP_NAME=food-tracker

# Application
NODE_ENV=development
NEXT_PUBLIC_APP_URL=http://localhost:3000

# PWA Configuration
NEXT_PUBLIC_PWA_CACHE_VERSION=1
CRON_SECRET=your-cron-secret
UNSPLASH_ACCESS_KEY=your-unsplash-access-key
BUCKET_COLLECTIBLES=your-bucket-collectibles

# Web Push VAPID keys (required for push notifications)
# Generate with: npx web-push generate-vapid-keys
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
# Used as the subject/owner identifier for VAPID. Can be a mailto: or URL.
VAPID_SUBJECT=mailto:you@example.com
AUTH_TRUST_HOST=true